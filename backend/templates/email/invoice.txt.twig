Bonjour {{ user.prenom }},

{% if isAttestation %}Vous trouverez ci-joint votre attestation de paiement pour la saison {{ season.name ?: season.id }}{% if season.startsAt and season.endsAt %} ({{ season.startsAt|date('d/m/Y') }} → {{ season.endsAt|date('d/m/Y') }}){% endif %}, votre cotisation ayant été réglée directement auprès de la FFTri (Espace Tri).{% else %}Vous trouverez ci-joint votre facture d'adhésion pour la saison {{ season.name ?: season.id }}{% if season.startsAt and season.endsAt %} ({{ season.startsAt|date('d/m/Y') }} → {{ season.endsAt|date('d/m/Y') }}){% endif %}.{% endif %}

Conservez-la pour vos remboursements employeur / mutuelle si concerné.

Sportivement,
L'équipe TTM
